{
  "compilerOptions": {
    "target": "es5", //指定要使用的默认库
    "module": "esnext", //在生成模块代码时指定模块系统。
    "baseUrl": "./", //解析非相关模块名称的基础目录
    "moduleResolution": "node", //指定如何解析导入模块。
    // 为了使 IntelliSense 使用 Webpack 别名，需要使用全局模式指定paths,设置要相对于 baseUrl 选项计算的路径映射
    "paths": {
      "@/*": [
        "src/*"
      ]
    },
    // 使用扩展库
    "lib": [
      "esnext",
      "dom",
      "dom.iterable",
      "scripthost"
    ]
  }
}
